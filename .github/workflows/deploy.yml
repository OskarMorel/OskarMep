name: deploy

on:
  workflow_dispatch:
    inputs:
      envdev:
        description: 'Choisir environnement de déploiement'
        required: true
        default: 'dev'

jobs:
  First_Job:
    name: first_job
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "Démarrage du workflow sur ${{github.ref}}"
      - name: recuperation code
        run: echo "Récupération de code"
        if: ${{github.ref == 'refs/heads/main'}}
